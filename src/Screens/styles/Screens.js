import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  viewStyle: {
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
  },
  textStyle: {
    fontSize: 18,
    fontWeight: 'bold',
  }
});
